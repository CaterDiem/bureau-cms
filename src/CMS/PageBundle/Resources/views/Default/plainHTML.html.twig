{# src/CMS/PageBundle/Resources/views/Default/plainHTML.html.twig #}
{# author: jtemplet #}
{# This is a wrapper template. Containers do the real work, using their layouts and templates and so on. #}

{% block contentWrapper %}
<div
    class="{{ cssClasses }}"    
    id="{{ block.name|lower|replace({' ': '_'}) }}{{ block.id }}"
    cms-block="true"
    cms-block-id="{{ block.id }}"
    cms-block-name="{{ block.name|lower|replace({' ': '_'}) }}"    
    cms-block-type="{{ type }}"
    {% if handler.blockIsEditable %}cms-block-editable="true"{% endif %}
    >
    {% block content %}
    {{ content|raw }}
    {% endblock content %}
</div>
{% endblock contentWrapper %}